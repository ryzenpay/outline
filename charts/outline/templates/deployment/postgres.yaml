apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: outline-postgresql
  namespace: {{ .Release.Namespace }}
spec:
  instances: 1
  storage:
    size: 5Gi
    storageClass: retain
  bootstrap:
    initdb:
      database: outline
      owner: outline